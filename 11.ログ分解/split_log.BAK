use strict;
use warnings;

while (my $line = <>) {
    next if $line =~ /^\n/;
    
    chomp $line;
    
    my ($col1, $col2, $col3) = split(/\s+/, $line);
    print $col1 . ',' . $col2 . ',' . $col3 ."\n";
}
